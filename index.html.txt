<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>For Sana 💕</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Cinzel:wght@400;600&display=swap" rel="stylesheet"/>
<style>
*{margin:0;padding:0;box-sizing:border-box}
body{
  min-height:100vh;
  background:radial-gradient(ellipse at 20% 20%, #2a003f 0%, #0d0018 40%, #1a0030 70%, #0a0015 100%);
  font-family:'Cormorant Garamond',Georgia,serif;
  overflow-x:hidden;
  cursor:default;
}
.glow-orb{
  position:fixed;top:50%;left:50%;transform:translate(-50%,-50%);
  width:70vw;height:70vh;
  background:radial-gradient(circle, rgba(255,20,147,0.12) 0%, rgba(224,64,251,0.06) 50%, transparent 70%);
  pointer-events:none;z-index:0;border-radius:50%;
  animation:orbPulse 4s ease-in-out infinite;
}
@keyframes orbPulse{0%,100%{opacity:0.6;transform:translate(-50%,-50%) scale(1)}50%{opacity:1;transform:translate(-50%,-50%) scale(1.08)}}
canvas{position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:1}
.main{position:relative;z-index:2;min-height:100vh;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:40px 20px}
.badge{
  display:inline-flex;align-items:center;gap:10px;
  padding:10px 22px;border-radius:30px;margin-bottom:36px;
  border:1px solid rgba(255,20,147,0.4);
  background:linear-gradient(135deg,rgba(255,20,147,0.1),rgba(224,64,251,0.07));
  animation:fadeDown 0.8s ease both;
}
.badge span{
  background:linear-gradient(90deg,#ff69b4,#fff,#e040fb,#ff1493);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
  font-size:13px;letter-spacing:1.5px;font-weight:400;
}
.layout{
  display:grid;grid-template-columns:1fr 1fr;gap:60px;align-items:center;
  max-width:1000px;width:100%;
}
@media(max-width:700px){
  .layout{grid-template-columns:1fr;gap:32px;text-align:center}
  .left,.right{display:flex;flex-direction:column;align-items:center}
}
.portrait-wrap{
  width:190px;height:190px;border-radius:50%;
  background:linear-gradient(135deg,#ff1493,#e040fb,#9c27b0);
  padding:4px;
  box-shadow:0 0 40px rgba(255,20,147,0.5),0 0 70px rgba(224,64,251,0.3);
  animation:float 4s ease-in-out infinite,fadeUp 1s ease 0.3s both;
  margin-bottom:28px;
}
@keyframes float{0%,100%{transform:translateY(-10px)}50%{transform:translateY(10px)}}
.portrait-inner{
  width:100%;height:100%;border-radius:50%;
  background:#1a0030;display:flex;align-items:center;justify-content:center;
  font-size:80px;
}
.sana-name{
  font-family:'Cinzel',serif;font-size:clamp(36px,6vw,58px);font-weight:400;
  letter-spacing:18px;line-height:1;
  background:linear-gradient(90deg,#ff69b4 0%,#fff 30%,#e040fb 60%,#ffd700 80%,#ff69b4 100%);
  background-size:200%;
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
  animation:shimmer 3s linear infinite,fadeUp 1s ease 0.5s both;
  margin-bottom:8px;
}
@keyframes shimmer{0%{background-position:0%}100%{background-position:200%}}
.subtitle{color:#ff69b4;font-size:14px;letter-spacing:4px;font-style:italic;animation:fadeUp 1s ease 0.7s both;margin-bottom:0}
.heart{
  font-size:64px;text-align:center;margin-bottom:28px;
  animation:heartbeat 0.9s ease-in-out infinite,fadeUp 1s ease 0.4s both;
  display:block;
}
@keyframes heartbeat{0%,100%{transform:scale(1)}50%{transform:scale(1.18)}}
.card{
  background:linear-gradient(135deg,rgba(255,20,147,0.08),rgba(224,64,251,0.05),rgba(156,39,176,0.08));
  border:1px solid rgba(255,20,147,0.28);
  border-radius:24px;padding:28px 28px 22px;
  animation:fadeUp 1s ease 0.6s both;
  transition:opacity 0.4s ease,transform 0.4s ease;
}
.card.hidden{opacity:0;transform:translateY(12px)}
.quote-mark{color:#ff1493;font-size:50px;line-height:0.8;font-family:Georgia,serif;margin-bottom:8px}
.card-title{
  font-family:'Cinzel',serif;font-size:19px;font-weight:600;letter-spacing:1px;margin-bottom:14px;
  background:linear-gradient(90deg,#ff69b4,#e040fb);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;
}
.card-body{color:rgba(255,255,255,0.8);font-size:15px;line-height:1.8;font-style:italic;letter-spacing:0.3px;white-space:pre-line}
.card-sign{color:rgba(255,105,180,0.75);font-size:12px;font-style:italic;text-align:right;margin-top:12px}
.reason{
  margin-top:20px;padding:14px 20px;
  background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.08);
  border-radius:16px;display:flex;align-items:center;justify-content:center;gap:10px;
  animation:fadeUp 1s ease 0.8s both;
}
.reason p{color:rgba(255,255,255,0.6);font-size:13px;letter-spacing:0.5px;font-style:italic;text-align:center}
.btn{
  margin-top:22px;display:inline-flex;align-items:center;gap:10px;
  padding:14px 32px;border-radius:50px;border:none;cursor:pointer;
  background:linear-gradient(90deg,#ff1493,#e040fb,#9c27b0);
  color:#fff;font-size:14px;font-family:'Cinzel',serif;font-weight:600;letter-spacing:1px;
  box-shadow:0 6px 24px rgba(255,20,147,0.4);
  transition:transform 0.2s,box-shadow 0.2s;
  animation:fadeUp 1s ease 0.9s both;
}
.btn:hover{transform:translateY(-2px) scale(1.03);box-shadow:0 10px 32px rgba(255,20,147,0.5)}
.btn:active{transform:scale(0.97)}
.dots{display:flex;gap:8px;justify-content:center;margin-top:18px;animation:fadeUp 1s ease 1s both}
.dot{
  height:8px;border-radius:4px;transition:all 0.35s ease;
  background:rgba(255,255,255,0.2);width:8px;
}
.dot.active{width:24px;background:linear-gradient(90deg,#ff1493,#e040fb)}
@keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeDown{from{opacity:0;transform:translateY(-16px)}to{opacity:1;transform:translateY(0)}}
</style>
</head>
<body>
<div class="glow-orb"></div>
<canvas id="c"></canvas>
<div class="main">
  <div class="badge">
    <span class="heart-sm">💕</span>
    <span>A message from your heart</span>
    <span class="heart-sm">💕</span>
  </div>

  <div class="layout">
    <!-- LEFT -->
    <div class="left">
      <div class="portrait-wrap">
        <div class="portrait-inner">🌹</div>
      </div>
      <div class="sana-name">S A N A</div>
      <p class="subtitle">— My everything —</p>
    </div>

    <!-- RIGHT -->
    <div class="right">
      <span class="heart">❤️</span>

      <div class="card" id="card">
        <div class="quote-mark">"</div>
        <div class="card-title" id="ctitle"></div>
        <div class="card-body" id="cbody"></div>
        <div class="card-sign">— With all my love 💕</div>
      </div>

      <div class="reason">
        <span>🌸</span>
        <p>You make every ordinary day extraordinary</p>
        <span>🌸</span>
      </div>

      <button class="btn" onclick="nextMsg()">💌 &nbsp;Next love note&nbsp; 💌</button>

      <div class="dots" id="dots"></div>
    </div>
  </div>
</div>

<script>
const messages=[
  {title:'My Universe',body:'In every star that lights the night,\nI see your eyes so warm and bright.\nYou are the poetry my heart writes\nin every breath, in quiet nights.'},
  {title:'My Reason',body:'Before you, life was just a story.\nWith you, it became a masterpiece.\nEvery morning I wake beside you\nis the greatest gift I will ever receive.'},
  {title:'Forever Yours',body:"If I could travel time and space,\nI'd choose every moment with your face.\nThrough every lifetime, every year —\nSana, you are why I am here."},
  {title:'My Home',body:"Home is not a place, it's a feeling.\nAnd every time I'm near you,\nI am exactly where I belong —\nforever safe, forever in love with you."},
];
let cur=0;
const emojis=['💕','🌹','✨','💖','🌸','💫','🦋','🌺'];

function setMsg(){
  const m=messages[cur];
  document.getElementById('ctitle').textContent=m.title;
  document.getElementById('cbody').textContent=m.body;
  const ds=document.getElementById('dots');
  ds.innerHTML='';
  messages.forEach((_,i)=>{
    const d=document.createElement('div');
    d.className='dot'+(i===cur?' active':'');
    ds.appendChild(d);
  });
}
function nextMsg(){
  const c=document.getElementById('card');
  c.classList.add('hidden');
  setTimeout(()=>{cur=(cur+1)%messages.length;setMsg();c.classList.remove('hidden');},380);
}
setMsg();

// Particle canvas
const canvas=document.getElementById('c');
const ctx=canvas.getContext('2d');
let W,H,particles=[];
function resize(){W=canvas.width=window.innerWidth;H=canvas.height=window.innerHeight;}
resize();
window.addEventListener('resize',resize);

function mkP(){
  return{
    x:Math.random()*W,y:Math.random()*H,
    size:Math.random()*16+9,speed:Math.random()*0.4+0.15,
    opacity:Math.random()*0.45+0.15,
    angle:Math.random()*Math.PI*2,
    emoji:emojis[Math.floor(Math.random()*emojis.length)],
    t:Math.random(),
  };
}
for(let i=0;i<22;i++)particles.push(mkP());

let frame=0;
function draw(){
  ctx.clearRect(0,0,W,H);
  frame+=0.005;
  particles.forEach(p=>{
    p.t=(p.t+p.speed*0.003)%1;
    const y=(1-p.t)*H;
    const x=p.x+Math.sin(frame*2+p.angle)*28;
    const fade=p.t<0.1?p.t/0.1:p.t>0.9?(1-p.t)/0.1:1;
    ctx.globalAlpha=p.opacity*fade;
    ctx.font=`${p.size}px serif`;
    ctx.fillText(p.emoji,x,y);
  });
  ctx.globalAlpha=1;
  requestAnimationFrame(draw);
}
draw();
</script>
</body>
</html>